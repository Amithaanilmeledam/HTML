<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Waterbody Risk Prediction</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <header>
      <h1>Manual Waterbody Safety Checker</h1>
      <p>
        Predict risk of Naegleria fowleri (amoeba infection) based on
        environment data
      </p>
    </header>

    <main class="content">
      <section class="explanation">
        <h2>Enter Environmental Details</h2>
        <form id="predictionForm" onsubmit="predictRisk(event)">
          <label for="location">Location Name (optional):</label><br />
          <input
            type="text"
            id="location"
            placeholder="e.g., Lake Victoria"
          /><br /><br />

          <label for="temperature">Air Temperature (°C):</label><br />
          <input
            type="number"
            id="temperature"
            required
            placeholder="e.g., 32"
          /><br /><br />

          <label for="waterType">Waterbody Type:</label><br />
          <select id="waterType" required>
            <option value="lake">Lake</option>
            <option value="pond">Pond</option>
            <option value="river">River</option>
            <option value="hot_spring">Hot Spring</option>
            <option value="canal">Canal</option></select
          ><br /><br />

          <label for="movement">Water Movement:</label><br />
          <select id="movement" required>
            <option value="still">Still</option>
            <option value="flowing">Flowing</option></select
          ><br /><br />

          <label for="rain">Recent Rainfall (last 3 days)?</label><br />
          <select id="rain" required>
            <option value="no">No</option>
            <option value="yes">Yes</option></select
          ><br /><br />

          <button class="btn" type="submit">Check Safety</button>
        </form>
      </section>

      <div id="result" class="result-box"></div>

      <section class="buttons">
        <a href="nasa.html" class="btn btn-secondary">← Back to Home</a>
      </section>
    </main>

    <footer>
      <p>© 2025 Amoeba Awareness Project. Think Before You Swim.</p>
    </footer>

    <script>
      function predictRisk(event) {
        event.preventDefault();

        const location = document.getElementById("location").value;
        const temp = parseFloat(document.getElementById("temperature").value);
        const waterType = document.getElementById("waterType").value;
        const movement = document.getElementById("movement").value;
        const rain = document.getElementById("rain").value;

        let riskMessage = "";
        let isRisk = false;

        // Simplified logic
        if (temp > 30 && movement === "still" && rain === "no") {
          isRisk = true;
          riskMessage = `<span style="color:red; font-weight:bold;">⚠️ High Risk:</span> Warm, still, and stagnant water with no recent rain can support Naegleria fowleri growth. Avoid swimming.`;
        } else {
          riskMessage = `<span style="color:green; font-weight:bold;">✅ Likely Safe:</span> Conditions are not favorable for the amoeba. Still, be cautious near warm water.`;
        }

        // Display result
        document.getElementById("result").innerHTML = `
        <h3>🧪 Prediction Result</h3>
        ${location ? `<p><strong>Location:</strong> ${location}</p>` : ""}
        <p><strong>Air Temp:</strong> ${temp} °C</p>
        <p><strong>Water Type:</strong> ${waterType}</p>
        <p><strong>Water Movement:</strong> ${movement}</p>
        <p><strong>Recent Rain:</strong> ${rain}</p>
        <p style="margin-top: 15px;">${riskMessage}</p>
      `;
      }
    </script>
  </body>
</html>
